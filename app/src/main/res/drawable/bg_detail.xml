<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <!-- shape如果不声明形状则默认为正方形 -->
        <shape>

            <!-- 圆角，你也可以对不同的角设置不同的数值 -->
            <corners android:radius="0.0dp"  />
            <!-- 形状的填充色 -->
            <solid android:color="@color/normal_press" />
            <!-- 边框宽度和颜色 -->
            <stroke android:width="0.0dp" android:color="@color/normal_press" />
        </shape>
    </item>
    <item android:drawable="@drawable/ic_line_d"/>
</selector>